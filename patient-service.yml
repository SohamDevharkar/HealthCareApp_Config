spring:
  application:
    name: patient-service

  h2:
    console:
      path: /h2-console

  eureka:
    instance:
      prefer-ip-address: true
    client:
      fetch-registry: true
      register-with-eureka: true
      service-url:
        default-zone: http://localhost:8761    
